<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <main>
        <% for (animal of animals){ %>
            <article>
                <h3>
                    <a href = "animals/<%= animal._id%>"><%= animal.species %></a> - <%= animal.extinct %> - <%= animal.location %> - <%= animal.lifeExpectancy %>
                </h3>
                <form action="/animals/<%= animal._id %>?_method=DELETE" method="POST">
                    <input type="submit" value="Delete"/></form>
            </article>

        <% } %>
        <br>
        <a href = "/animals/new"><button>Add an animal</button></a>
    </main>
</body>
</html>